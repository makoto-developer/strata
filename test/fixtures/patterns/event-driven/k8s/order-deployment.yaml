apiVersion: apps/v1
kind: Deployment
metadata:
  name: order
spec:
  template:
    spec:
      containers:
        - name: app
          image: example/order:latest
          env:
            - name: KAFKA_TOPIC
              value: orders.placed
            - name: LOG_LEVEL
              value: info
