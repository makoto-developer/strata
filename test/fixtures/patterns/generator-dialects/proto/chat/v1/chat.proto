syntax = "proto3";
package acme.chat.v1;
option go_package = "example.com/acme/proto/chat/v1;chatv1";

service ChatService {
  rpc Send(SendRequest) returns (SendResponse);
}

message SendRequest { string body = 1; }
message SendResponse { string id = 1; }
