syntax = "proto3";
package acme.notify.v1;
option go_package = "example.com/acme/proto/notify;notifypb";

service NotifyService {
  rpc Push(PushRequest) returns (PushResponse);
}

message PushRequest { string body = 1; }
message PushResponse { bool ok = 1; }
